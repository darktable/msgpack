To build for .NET Framework use Visual Studio 2010.

To build for Mono you need to install Nant. Run the following command to build:
mono {path_to_nant.exe}

Then find MsgPack.dll in the "output" folder.


